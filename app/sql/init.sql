CREATE TABLE Users (
	phone_no VARCHAR(8) PRIMARY KEY,
    password TEXT NOT NULL
);

CREATE TABLE pet_owner (
	phone_no VARCHAR(8) PRIMARY KEY,
    password TEXT NOT NULL
);

CREATE TABLE care_taker (
	phone_no VARCHAR(8) PRIMARY KEY,
    password TEXT NOT NULL
);
